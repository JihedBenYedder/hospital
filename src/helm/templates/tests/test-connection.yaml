apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hospitalms.fullname" . }}-test-connection"
  labels:
    {{- include "hospitalms.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hospitalms.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
